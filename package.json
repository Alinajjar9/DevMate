{
  "name": "devmate",
  "displayName": "DevMate",
  "description": "A local-first VS Code coding assistant with project tools, sessions, and model profiles.",
  "version": "1.0.0",
  "publisher": "local",
  "author": "Ali Najjar",
  "private": true,
  "vsce": {
    "readmePath": "VSIX_README.md"
  },
  "engines": {
    "vscode": "^1.96.2",
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:devMate.openChat",
    "onView:devmate.dedicatedAssistantView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "devMate.openChat",
        "title": "DevMate: Open Chat",
        "category": "DevMate"
      }
    ],
    "viewsContainers": {
      "secondarySidebar": [
        {
          "id": "devmate-dedicated-chat",
          "title": "DevMate",
          "icon": "media/devmate.svg"
        }
      ]
    },
    "views": {
      "devmate-dedicated-chat": [
        {
          "type": "webview",
          "id": "devmate.dedicatedAssistantView",
          "name": "Chat",
          "contextualTitle": "DevMate",
          "icon": "media/devmate.svg"
        }
      ]
    },
    "configuration": {
      "title": "DevMate",
      "properties": {
        "devMate.backendUrl": {
          "type": "string",
          "default": "http://127.0.0.1:8000",
          "description": "Base URL of the local DevMate backend service."
        },
        "devMate.manageLocalBackend": {
          "type": "boolean",
          "default": true,
          "description": "Automatically start and monitor the bundled backend when backendUrl is a local HTTP address."
        },
        "devMate.backendPythonPath": {
          "type": "string",
          "default": "",
          "description": "Optional Python executable used for the managed local backend. DevMate otherwise prefers its project virtual environment and then Python on PATH."
        },
        "devMate.maxTokens": {
          "type": "number",
          "default": 16384,
          "minimum": 128,
          "maximum": 32000,
          "description": "Maximum response tokens shared by model reasoning, answers, and complete Code-mode file contents."
        },
        "devMate.temperature": {
          "type": "number",
          "default": 0.2,
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature for model requests."
        },
        "devMate.requestTimeoutSeconds": {
          "type": "integer",
          "default": 900,
          "minimum": 10,
          "maximum": 1800,
          "description": "How long DevMate allows each model-provider call to run. The extension automatically adds a 30-second transport buffer."
        },
        "devMate.commandTimeoutSeconds": {
          "type": "integer",
          "default": 300,
          "minimum": 10,
          "maximum": 1800,
          "description": "Maximum runtime for each approved DevMate verification command."
        },
        "devMate.toolCallLimit": {
          "type": "integer",
          "default": 16,
          "minimum": 4,
          "maximum": 100,
          "description": "Maximum project-tool calls in one DevMate request. Higher values can increase runtime, provider cost, and loop risk."
        },
        "devMate.readFileMaxLines": {
          "type": "integer",
          "default": 400,
          "minimum": 100,
          "maximum": 1000,
          "description": "Maximum lines returned by one read_file agent-tool call."
        },
        "devMate.listFilesMaxResults": {
          "type": "integer",
          "default": 200,
          "minimum": 20,
          "maximum": 500,
          "description": "Maximum file paths returned by one list_files call."
        },
        "devMate.searchCodeMaxResults": {
          "type": "integer",
          "default": 50,
          "minimum": 10,
          "maximum": 200,
          "description": "Maximum matches returned by one search_code call."
        },
        "devMate.diagnosticsMaxResults": {
          "type": "integer",
          "default": 100,
          "minimum": 10,
          "maximum": 300,
          "description": "Maximum Problems entries returned by one get_diagnostics call."
        },
        "devMate.terminalErrorsMaxResults": {
          "type": "integer",
          "default": 5,
          "minimum": 1,
          "maximum": 10,
          "description": "Maximum failed terminal commands returned by read_terminal_errors."
        },
        "devMate.codeNavigationMaxResults": {
          "type": "integer",
          "default": 100,
          "minimum": 10,
          "maximum": 300,
          "description": "Maximum locations returned by get_symbols, find_definition, or find_references."
        },
        "devMate.configuration": {
          "type": "object",
          "default": {},
          "scope": "window",
          "additionalProperties": false,
          "properties": {
            "maxTokens": {
              "type": "integer",
              "minimum": 128,
              "maximum": 32000,
              "description": "Maximum output tokens for one provider response."
            },
            "temperature": {
              "type": "number",
              "minimum": 0,
              "maximum": 2,
              "description": "Sampling temperature, when accepted by the model."
            },
            "timeoutSeconds": {
              "type": "integer",
              "minimum": 10,
              "maximum": 1800,
              "description": "Timeout for each provider call in seconds."
            },
            "commandTimeoutSeconds": {
              "type": "integer",
              "minimum": 10,
              "maximum": 1800,
              "description": "Maximum runtime of an approved verification command."
            },
            "toolCallLimit": {
              "type": "integer",
              "minimum": 4,
              "maximum": 100,
              "description": "Total tool calls allowed in one request."
            },
            "maxFileEdits": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Maximum file-changing calls in one request; 0 disables them."
            },
            "maxCommands": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Maximum verification command calls; 0 disables them."
            },
            "maxRepairAttempts": {
              "type": "integer",
              "minimum": 1,
              "maximum": 10,
              "description": "Failed attempts on the same tool and target before stopping."
            },
            "contextCharacters": {
              "type": "integer",
              "minimum": 1000,
              "maximum": 40000,
              "description": "Maximum characters of code in starting context."
            },
            "historyCharacters": {
              "type": "integer",
              "minimum": 10000,
              "maximum": 80000,
              "description": "Maximum characters retained in tool-result history."
            },
            "runTokenBudget": {
              "type": "integer",
              "minimum": 0,
              "maximum": 5000000,
              "description": "Cumulative request token budget; 0 disables it. Estimated checks stop before another call, but are not a billing cap."
            },
            "instructions": {
              "type": "string",
              "maxLength": 12000,
              "description": "Project or personal instructions. Tool restrictions remain enforced."
            },
            "pinnedFiles": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "maxItems": 5,
              "description": "Workspace-relative files included before automatic context retrieval."
            },
            "excludedPaths": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "maxItems": 100,
              "description": "Paths or *, ** and ? patterns excluded from starting context. This does not restrict tool access."
            },
            "enabledTools": {
              "type": "array",
              "uniqueItems": true,
              "items": {
                "type": "string"
              },
              "description": "Tools available to the agent, further restricted by mode and permissions."
            }
          },
          "description": "Agent defaults and project overrides. Adjust model values and advanced limits in DevMate settings."
        }
      }
    }
  },
  "scripts": {
    "build:backend": "node scripts/build-backend.js",
    "clean": "node scripts/clean-out.js",
    "compile": "npm run clean && tsc -p ./ --listEmittedFiles",
    "check": "tsc -p ./ --noEmit",
    "test": "npm run verify",
    "test:extension": "npm run compile && node scripts/run-extension-tests.js",
    "test:backend": "node scripts/run-backend-tests.js",
    "test:contracts": "node scripts/check-contracts.js",
    "verify:out": "node scripts/verify-out.js",
    "verify": "npm run check && npm run test:extension && npm run test:backend && npm run test:contracts && npm run verify:out",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run build:backend && npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.14.8",
    "@types/vscode": "^1.96.2",
    "typescript": "^5.4.5"
  }
}
